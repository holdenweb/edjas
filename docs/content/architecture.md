Four diagrams describe how EDJAS is put together, zooming in a level at a time: the
setting it sits in, the pieces that make it up, the components inside the library, and
finally the modules and the path a single extraction takes through them.
